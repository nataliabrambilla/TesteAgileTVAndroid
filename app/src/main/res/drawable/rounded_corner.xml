<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="16dp" />

    <!-- Cor de fundo do shape, usando um atributo de tema -->
    <solid android:color="?attr/colorSurface" /> <!-- Usa a cor de superfície do tema -->

    <!-- Cor da borda do shape, usando um atributo de tema -->
    <stroke
        android:width="1dp"
        android:color="?attr/colorOnSurface" />

</shape>